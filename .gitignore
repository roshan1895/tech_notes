# ── Jekyll build output & cache ──────────────────────────────
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# ── Ruby / Bundler ───────────────────────────────────────────
vendor/
.bundle/
# Gemfile.lock is intentionally committed (locks local build versions)

# ── Node (stacknotes Astro project) ──────────────────────────
node_modules/
stacknotes/dist/
stacknotes/.astro/

# ── Environment / secrets ────────────────────────────────────
.env
.env.*
*.local

# ── OS / editor cruft ────────────────────────────────────────
.DS_Store
**/.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/
