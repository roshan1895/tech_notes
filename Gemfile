source "https://rubygems.org"

gem "jekyll", "~> 4.0"

# GitHub Pages whitelisted plugins — kept in a jekyll_plugins group so local
# `bundle exec jekyll build` matches what GitHub Pages runs.
group :jekyll_plugins do
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
  gem "jekyll-feed"
end
